- name: Create user
  hosts: devservers
  remote_user: devops
  become: yes
  vars_files:
    - vars/secret.yml
  tasks:
    - name: Create user
      user:
        name: "{{ item.name }}"
        password: "{{ item.pw | password_hash('sha512')}}"
      with_items: "{{ newusers }}"
